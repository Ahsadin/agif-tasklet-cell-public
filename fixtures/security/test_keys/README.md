# Test Key Fixtures

These files are non-production placeholder fixtures only.

Rules:
- Never use these files for real signing or licensing.
- Real private keys must never be committed.
- This public repository ships placeholders only, not reusable key material.
- If a local test needs a real key, generate it ephemerally outside git.
