# v4 Model Assets

This folder stores deterministic local model artifacts for `finance_doc_extractor_neural_v4`.

- `transformer_v4.onnx`: v4 pseudo-ONNX Int8 artifact (CPU-only metadata contract).
- `transformer_v4_metadata.json`: runtime + quantization metadata.
- `export_manifest.json`: export provenance for reproducible rebuilds.
